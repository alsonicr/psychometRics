# The random intercept crosslag panel model

The random intercept crosslag panel model

## Usage

``` r
RI_crosslag_model(variables, times, order = "times/variables", join = "_")
```

## Arguments

- join:
